name: Deploy

on:
  push:
    branches: [main]

# Required for GitHub Pages deployment
permissions:
  contents: read
  pages: write
  id-token: write

# Only allow one deployment at a time; cancel in-progress if a new push arrives
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      # Rebuild manifests at deploy time — no commit needed
      - name: Rebuild manifests
        run: node .github/scripts/rebuild-manifests.js

      # Download local fallback copies of all Cloudinary images
      # Non-fatal: if Cloudinary is unreachable, the site still works via CDN URLs
      - name: Sync Cloudinary images to local fallback
        run: node .github/scripts/sync-images.js

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
